<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About Us | Pulse & Power Co.</title>

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet">

    <style>
        :root {
            --summit-green: #004d40;
            --bolt-yellow: #fdd835;
            --slate-gray: #37474f;
            --white: #ffffff;
        }

        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            background: var(--white);
            color: var(--slate-gray);
            line-height: 1.6;
        }

        .hero {
            background: linear-gradient(135deg, #00352d, var(--summit-green));
            color: var(--white);
            padding: 100px 20px;
            text-align: center;
        }

        .hero h1 {
            font-size: 4rem;
            color: var(--bolt-yellow);
            letter-spacing: 3px;
            font-weight: 900;
        }

        .hero p {
            font-size: 1.4rem;
            max-width: 700px;
            margin: 20px auto 0;
        }

        .section {
            padding: 80px 12%;
        }

        .section h2 {
            color: var(--summit-green);
            font-size: 2.5rem;
            margin-bottom: 20px;
        }

        .values {
            display: flex;
            gap: 40px;
            margin-top: 40px;
            flex-wrap: wrap;
        }

        .value-card {
            background: #f4f4f4;
            padding: 30px;
            border-radius: 12px;
            flex: 1;
            min-width: 220px;
        }

        .value-card h3 {
            color: var(--summit-green);
            margin-bottom: 10px;
        }

        .cta {
            bac
